  spring:
    config:
      activate:
        on-profile: prod
    datasource:
      url: jdbc:mysql://${DB_HOST:db}:${DB_PORT:3306}/${DB_NAME:treehole}?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Tokyo&allowPublicKeyRetrieval=true
      username: ${DB_USER:treehole_user}
      password: ${DB_USER_PASSWORD:treehole_pass}
      driver-class-name: com.mysql.cj.jdbc.Driver
